/*
import pkg from "pg";
const { Pool } = pkg;

const pool = new Pool({
  user: "omarshatat",
  host: "localhost",
  database: "express_test",
  password: "", // no password
  port: 5432, // default PostgreSQL port
});

export default pool;
*/